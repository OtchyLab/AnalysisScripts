% Close the figure window.
if exist('hFigA')
    if ishandle(hFigA) %if a valid graphics object
        delete(hFigA);   
    end
end
if exist('hFig_ctrA')
    if ishandle(hFig_ctrA)
        delete(hFig_ctrA);   
    end
end
